{
  "id": "ast-grep",
  "version": "1.0.2",
  "name": "ast-grep(sg)",
  "description": "Installs ast-grep for searching and refactoring code",
  "options": {
    "VERSION": {
      "type": "string",
      "description": "ast-grep version",
      "default": "latest"
    }
  }
}
